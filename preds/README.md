# Predictions

This folder will contain the generated predictions from `scripts/forecast.py` (e.g. `valid_predictions.csv` and `test_predictions.csv`), which are then consumed by `scripts/optimize.py`.
